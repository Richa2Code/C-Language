#include <stdio.h>

int main()
{
  // Arithmetic instructions.
  // POW(X,Y) FOR X TO THE POWER Y
  int a, b;
  int sum = a + b;
  int power = pow(b, c);
  return 0;
  // Single veriable on the left hand side.
  // int a+b = sum :this statment is invalid.because there is no single veriable.
  /*INVALID
    B + C = A
    A = BC
    A = B^C
  */
}